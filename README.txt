# Main packages should be install globally and child package (package that depends on another
# package like plugins for postcss and loaders for webpack")

#create src/style.css for tailwind css and dist/style.css for output